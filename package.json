{
  "name": "earthscigen",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-only synthetic Earth Science preprint scaffold generator.",
  "license": "MIT",
  "scripts": {
    "check": "node --check assets/app.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
